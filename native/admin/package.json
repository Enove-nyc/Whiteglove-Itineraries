{
  "name": "wg-admin-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native shell (Capacitor) for the White Glove Admin app. Loads the live planner app in Android's own WebView — no Chrome, no address bar, full control of the system bars, real device permissions.",
  "scripts": {
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
